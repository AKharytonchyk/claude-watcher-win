# Constitution

This repo uses **GitHub Spec Kit**, so the canonical constitution lives where the
`/speckit-*` workflow reads it:

➡️ **[.specify/memory/constitution.md](.specify/memory/constitution.md)**

It reproduces the macOS app's constitution for **Windows + WSL** — six principles
(privacy-first, never steal focus, lean & Claude-focused, native-not-templated,
verify before shipping, small stays small), plus platform constraints and the
spec-driven workflow. Amend it there (or via `/speckit-constitution`), not here.
